{
  "name": "movie-library",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"pnpm --filter movie-library-client dev\" \"pnpm --filter movie-library-server dev\"",
    "build": "pnpm --filter movie-library-client build && pnpm --filter movie-library-server build",
    "start": "pnpm --filter movie-library-server start",
    "lint": "pnpm --filter movie-library-client lint",
    "migrate": "pnpm --filter movie-library-server migrate"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
